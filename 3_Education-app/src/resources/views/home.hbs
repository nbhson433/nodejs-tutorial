<h3 class="mt-3">{{title}}</h1>
<hr class="dropdown-divider">

<div class="mt-3">
  <div class="row">

    {{#each courses}}
      <div class="col-sm-6 col-md-4 col-lg-3">
        <div class="card mb-3 shadow-sm">
          <img src="{{this.image}}" class="card-img-top" alt="..." />
          <div class="card-body">
            <h5 class="card-title"><a href="/courses/{{this.slug}}">{{this.name}}</a></h5>
            <p class="card-text card-text-detail">{{this.description}}</p>
            <a href="/courses/{{this.slug}}" class="btn btn-primary">Chi tiết <i class="fas fa-share"></i></a>
          </div>
        </div>
      </div>
    {{/each}}

  </div>
</div>